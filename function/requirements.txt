# Azure Functions remote builds consume this file. Keep the runtime dependency
# graph in the generated lock and require every downloaded artifact to match a
# reviewed SHA-256 hash.
--require-hashes
-r requirements.lock
